// this is form to export the content from this moudle (file) to another moudel(file)

const a = 'Raghunath';
const b = 'Das';
const c = 'liku';
const d = 'RD';

export default d;  // default hence in import module we can use any name as reference
export {a};  // named export so need to use same name in import moudle
export {b};
export {c};
